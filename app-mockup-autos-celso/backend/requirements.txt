# Backend - API de Predicción de Precios de Vehículos
# Versiones mínimas compatibles con Python 3.11, 3.12 y 3.13

# ── Framework web ─────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ── Validación de datos ───────────────────────────────────
pydantic>=2.7.0

# ── Machine Learning ─────────────────────────────────────
# IMPORTANTE: scikit-learn debe coincidir con la versión usada para entrenar el modelo.
# Si la versión instalada difiere, puede haber incompatibilidades al cargar el .pkl.
# Versión recomendada: la misma que se usó en el entrenamiento.
scikit-learn>=1.4.0
joblib>=1.3.0

# ── Procesamiento de datos ────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0

# ── Variables de entorno ──────────────────────────────────
python-dotenv>=1.0.0
